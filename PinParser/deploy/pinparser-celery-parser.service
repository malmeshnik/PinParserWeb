[Unit]
Description=PinParser Celery Parser Worker
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/PinParserWeb/PinParser
Environment="PATH=/root/PinParserWeb/PinParser/venv/bin"
ExecStart=/root/PinParserWeb/PinParser/venv/bin/celery -A config worker -Q parser --loglevel=info --concurrency=4 -n parser@%h
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
